---
import arrowLeft from "../../images/icon/arrow-left.svg";

interface Props {
  hasIcon?: boolean;
  class?: string;
}

const { hasIcon = true, class: className, ...rest } = Astro.props;
---

<button
  class:list={[
    "border-primary-light bg-primary-light flex shrink-0 cursor-pointer items-center justify-center gap-[10px] rounded-[8px] border tracking-[-0.02em] text-white transition-opacity hover:opacity-80",
    className,
  ]}
  {...rest}
>
  <slot />
  {
    hasIcon && (
      <div
        class="relative size-[14px] bg-white"
        style={`mask-image: url(${arrowLeft.src});`}
      />
    )
  }
</button>
